export const metadata = {
  title: 'Process - Kyle McGraw',
  description: 'Structured methodology, flexible to your goals. A clear approach that turns ambiguity into aligned decisions.',
}

export const sections = [
  { title: 'My Process', id: 'my-process' },
]

import { ProcessFlow } from '@/components/ProcessFlow'

# My Process

Structured methodology, flexible to your goals. Phases scale up or down depending on what the project actually needs. {{ className: 'lead' }}

<ProcessFlow />
export const dynamic = 'error'
