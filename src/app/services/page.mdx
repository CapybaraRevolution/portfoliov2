export const metadata = {
  title: 'Services - Kyle McGraw',
  description: 'Product Manager, UX Design, and Business Analysis services powered by proven methodologies for exceptional digital experiences.',
}

import { Button } from '@/components/Button'
import { ProcessRedirect } from '@/components/ProcessRedirect'
import { SimplifiedServicesGrid } from '@/components/SimplifiedServicesGrid'
import { RaycastFigureCard, RaycastInspiredGallery } from '@/components/RaycastInspiredGallery'
import { AnimatedBeamMultipleOutputDemo } from '@/components/AnimatedBeamMultipleOutputDemo'
import { ThreeDMarqueeDemo } from '@/components/ThreeDMarqueeDemo'
import Image from 'next/image'

<ProcessRedirect />

<section className="not-prose py-16 sm:py-20">
  <div className="mx-auto max-w-6xl px-6 lg:px-8">
    <div className="relative overflow-hidden rounded-3xl border border-zinc-900/5 bg-white/80 shadow-xl ring-1 ring-white/80 backdrop-blur-sm dark:border-white/10 dark:bg-zinc-900/50 dark:ring-white/5">
      {/* Soft aura inspired by the about-page treatment */}
      <div className="pointer-events-none absolute -inset-6 rounded-[28px] bg-gradient-to-br from-emerald-200/25 via-white/10 to-sky-200/25 blur-3xl dark:from-emerald-500/15 dark:via-emerald-900/10 dark:to-sky-500/15" aria-hidden="true" />
      {/* Subtle grid sheen */}
      <div
        className="pointer-events-none absolute inset-0 opacity-25 mix-blend-overlay"
        aria-hidden="true"
        style={{
          backgroundImage:
            'linear-gradient(to right, rgba(16,185,129,0.12) 1px, transparent 1px), linear-gradient(to bottom, rgba(16,185,129,0.12) 1px, transparent 1px)',
          backgroundSize: '32px 32px',
        }}
      />
      <div className="pointer-events-none absolute inset-0 bg-gradient-to-br from-emerald-50/60 via-transparent to-emerald-100/30 dark:from-emerald-500/10 dark:via-transparent dark:to-emerald-400/5" />
      <Image
        src="/images/samples/wireframes/Annotated-Wireframe_Hero.png"
        alt="Annotated wireframe with notes and highlights across a flow."
        width={4752}
        height={2016}
        className="h-full w-full object-cover"
        priority
        sizes="(min-width: 1024px) 1100px, 100vw"
      />
    </div>

    <div className="mt-12 max-w-4xl">
      <p className="text-sm font-semibold uppercase tracking-wide text-emerald-600 dark:text-emerald-400">
        {'Services'}
      </p>
      <h1 className="mt-3 text-balance text-4xl font-semibold tracking-tight text-zinc-900 dark:text-white sm:text-5xl">
        {'Turning messy problem spaces into clear, testable ideas.'}
      </h1>
      <p className="mt-6 max-w-3xl text-lg text-zinc-600 dark:text-zinc-300">
        {`I am a hybrid UX designer and business analyst who helps teams align fast, map complex systems, and test what matters before committing code. Every engagement ends with clarity, evidence, and a confident next move.`}
      </p>
      <div className="mt-8 flex flex-wrap gap-4">
        <Button href="/contact" arrow="right">
          {'Schedule a call'}
        </Button>
        <Button href="/work/overview" variant="secondary" arrow="right">
          {'View case studies'}
        </Button>
      </div>
    </div>
  </div>
</section>

<section className="not-prose mx-auto -mt-6 max-w-5xl px-6 lg:px-8">
  <RaycastFigureCard
    figureId="systems-audit"
    eyebrow="FIG_01"
    title="Map dependencies before scope"
    description="A quick peek at how I surface integrations, constraints, and risk areas so teams make cleaner build decisions."
  />
</section>

<section className="not-prose mx-auto my-12 max-w-6xl px-6 lg:px-8">
  <div className="flex flex-col gap-6 lg:flex-row lg:items-start lg:justify-between">
    <div className="max-w-xl">
      <p className="text-sm font-semibold uppercase tracking-wide text-emerald-600 dark:text-emerald-400">
        {'How the work looks in motion'}
      </p>
      <h2 className="mt-2 text-3xl font-semibold tracking-tight text-zinc-900 dark:text-white sm:text-4xl">
        {'Visual snapshots of alignment, structure, and testing.'}
      </h2>
      <p className="mt-3 text-base text-zinc-600 dark:text-zinc-300">
        {`Each engagement is grounded in quick, visual artifacts. They keep teams aligned and surface risks early—before code or budget goes sideways.`}
      </p>
    </div>
  </div>

  <div className="mt-8 grid grid-cols-1 gap-6 lg:grid-cols-3">
    <RaycastFigureCard
      figureId="systems-audit"
      eyebrow="FIG_01"
      title="Systems audit"
      description="Dependencies, integrations, and constraints mapped for confident scope."
      size="compact"
    />
    <RaycastFigureCard
      figureId="ui-blueprint"
      eyebrow="FIG_02"
      title="UI blueprint"
      description="States, flows, and reuse patterns—so UX, product, and eng stay in lockstep."
      size="compact"
    />
    <RaycastFigureCard
      figureId="experiment-loop"
      eyebrow="FIG_03"
      title="Experiment loop"
      description="Hypotheses, signals, and rollout states visualized to de-risk decisions fast."
      size="compact"
    />
  </div>
</section>

<section className="not-prose mx-auto my-12 max-w-6xl px-6 lg:px-8">
  <div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
    <div>
      <p className="text-sm font-semibold uppercase tracking-wide text-emerald-600 dark:text-emerald-400">
        {'UI moves in motion'}
      </p>
      <h3 className="mt-2 text-2xl font-semibold tracking-tight text-zinc-900 dark:text-white">
        {'A quick tour of interactive patterns I ship.'}
      </h3>
      <p className="mt-2 text-sm text-zinc-600 dark:text-zinc-300">
        {`A rotating gallery of micro-interactions, system cards, and experiments that keep teams curious and aligned.`}
      </p>
    </div>
  </div>
  <ThreeDMarqueeDemo />
</section>

<div className="not-prose">
  <SimplifiedServicesGrid />
</div>
<section className="not-prose py-16 sm:py-20">
  <div className="mx-auto max-w-6xl px-6 lg:px-8">
    <div className="max-w-3xl">
      <p className="text-sm font-semibold uppercase tracking-wide text-emerald-600 dark:text-emerald-400">
        {'How I work'}
      </p>
      <h2 className="mt-3 text-3xl font-semibold tracking-tight text-zinc-900 dark:text-white sm:text-4xl">
        {'Clarity first, then momentum.'}
      </h2>
      <p className="mt-4 text-base text-zinc-600 dark:text-zinc-300">
        {`Each engagement is designed to unblock decisions quickly while keeping everyone aligned around the same story.`}
      </p>
    </div>

    <div className="mt-10 grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-4">
      {[
        {
          title: 'Align',
          body: 'Frame the problem, surface constraints, and agree on success signals.',
        },
        {
          title: 'Map',
          body: 'Make the invisible visible—flows, systems, decision points, and risks.',
        },
        {
          title: 'Test',
          body: 'Prototype or research the riskiest assumptions with clear metrics.',
        },
        {
          title: 'Decide',
          body: 'Share a decision-ready readout with next moves, owners, and timelines.',
        },
      ].map((step) => (
        <div
          key={step.title}
          className="rounded-2xl border border-zinc-900/5 bg-white/70 p-6 shadow-sm dark:border-white/10 dark:bg-zinc-900/60"
        >
          <h3 className="text-lg font-semibold text-zinc-900 dark:text-white">{step.title}</h3>
          <p className="mt-3 text-sm text-zinc-600 dark:text-zinc-400">{step.body}</p>
        </div>
      ))}
    </div>


    <div className="mt-12 grid grid-cols-1 items-start gap-8 lg:grid-cols-2">
      <div className="space-y-4">
        <h3 className="text-lg font-semibold text-zinc-900 dark:text-white">{'Wire the experience'}</h3>
        <p className="text-sm text-zinc-600 dark:text-zinc-400">
          {`Before tickets, I map the scaffolding—navigation, states, and component reuse—so design, product, and engineering stay aligned.`}
        </p>
        <div className="rounded-2xl border border-emerald-100/70 bg-emerald-50/40 p-4 text-sm text-emerald-800 shadow-sm dark:border-emerald-900/40 dark:bg-emerald-900/20 dark:text-emerald-100">
          <p className="font-semibold">{'Deliverables you see quickly'}</p>
          <ul className="mt-2 space-y-1 text-emerald-900/80 dark:text-emerald-50/80">
            <li>{'• Alignment map with risks and constraints'}</li>
            <li>{'• Interaction blueprint across key states'}</li>
            <li>{'• Experiment path with signals and owners'}</li>
          </ul>
        </div>
      </div>
      <div className="flex flex-col gap-6">
        <RaycastFigureCard
          figureId="ui-blueprint"
          eyebrow="FIG_02"
          title="UI blueprint"
          description="Structure, state, and reuse patterns captured visually to prevent rework."
          size="compact"
        />
        <div className="rounded-2xl border border-emerald-100/70 bg-white/70 p-4 shadow-sm dark:border-emerald-900/40 dark:bg-zinc-900/70">
          <p className="text-xs font-semibold uppercase tracking-wide text-emerald-600 dark:text-emerald-400">
            {'How it flows across tools'}
          </p>
          <AnimatedBeamMultipleOutputDemo className="mt-4" />
        </div>
      </div>
    </div>
  </div>
</section>

<section className="not-prose py-16 sm:py-20">
  <div className="mx-auto max-w-6xl px-6 lg:px-8">
    <div className="max-w-3xl">
      <p className="text-sm font-semibold uppercase tracking-wide text-emerald-600 dark:text-emerald-400">
        {'Engagement models'}
      </p>
      <h2 className="mt-3 text-3xl font-semibold tracking-tight text-zinc-900 dark:text-white sm:text-4xl">
        {'Start where you are—move with intent.'}
      </h2>
      <p className="mt-4 text-base text-zinc-600 dark:text-zinc-300">
        {`Choose the pace and depth that matches your team. Each model includes clear deliverables and a crisp decision point.`}
      </p>
    </div>

    <div className="mt-10 grid grid-cols-1 gap-8 md:grid-cols-3">
      {[
        {
          title: 'Workshop Sprint',
          duration: '1–2 weeks',
          items: [
            'Facilitated workshops to align on goals and risks',
            'Artifacts: maps, flows, and prioritized bets',
            'Decision: go/no-go criteria + next steps',
          ],
        },
        {
          title: 'Project Engagement',
          duration: '4–8 weeks',
          items: [
            'End-to-end design, research, and requirements',
            'Prototypes, specs, and usability findings',
            'Decision: build-ready scope with owners and dates',
          ],
        },
        {
          title: 'Product Advisory',
          duration: 'Ongoing',
          items: [
            'Weekly/bi-weekly cadence with leadership and teams',
            'Rapid feedback on roadmaps, flows, and experiments',
            'Decision: keep momentum without losing clarity',
          ],
        },
      ].map((model) => (
        <div
          key={model.title}
          className="flex h-full flex-col rounded-2xl border border-zinc-900/5 bg-white/70 p-6 shadow-sm dark:border-white/10 dark:bg-zinc-900/60"
        >
          <div className="flex items-baseline justify-between gap-4">
            <h3 className="text-lg font-semibold text-zinc-900 dark:text-white">{model.title}</h3>
            <span className="text-xs font-semibold uppercase tracking-wide text-emerald-700 dark:text-emerald-300">
              {model.duration}
            </span>
          </div>
          <ul className="mt-4 space-y-3 text-sm text-zinc-600 dark:text-zinc-400">
            {model.items.map((item) => (
              <li key={item} className="flex gap-2">
                <span aria-hidden="true" className="text-emerald-500 dark:text-emerald-300">
                  •
                </span>
                <span>{item}</span>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>

    <div className="mt-12">
      <RaycastFigureCard
        figureId="experiment-loop"
        eyebrow="FIG_03"
        title="Experiment loop"
        description="How we frame hypotheses, signals, and rollout states to reduce risk before committing full build."
      />
    </div>
  </div>
</section>

<section className="not-prose py-16 sm:py-20">
  <div className="mx-auto max-w-5xl px-6 text-center lg:px-8">
    <h2 className="text-3xl font-semibold tracking-tight text-zinc-900 dark:text-white sm:text-4xl">
      {'Ready to bring clarity to your roadmap?'}
    </h2>
    <p className="mt-4 text-base text-zinc-600 dark:text-zinc-300">
      {`Tell me where you are stuck—alignment, research, prototyping, or requirements—and we will design the smallest step that unlocks your next decision.`}
    </p>
    <div className="mt-8 flex flex-wrap justify-center gap-4">
      <Button href="/contact" arrow="right">
        {'Schedule a call'}
      </Button>
      <Button href="/work/overview" variant="secondary" arrow="right">
        {'View case studies'}
      </Button>
    </div>
  </div>
</section>
export const dynamic = 'force-dynamic'
